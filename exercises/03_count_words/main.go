package main

/*
import (
	"fmt"
	"io/ioutil"
	"log"
	"strings"
)
*/

func main() {
	// TODO: Ask the user to enter the file name

	// Read the file
	/*
		content, err := ioutil.ReadFile(fileName)
		if err != nil {
			log.Fatalf("Error reading the file: %v", err)
		}
	*/

	// TODO: Convert the file content to a string and split it into words with strings.Fields()

	// TODO: Use a map to store the the words and their number of occurrences, print the result
}
