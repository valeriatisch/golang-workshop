package main

// Create type "Currency"

// Create constants of this type: USD = 1.0, EUR = 0.91, GBP = 0.79

/*
	func convert(amount Currency, from Currency, to Currency) Currency {
		return (amount / from) * to
	}
*/

func main() {
	// TODO: Ask the user for the amount, its currency and the target currency

	// TODO: Use switch to check if the currencies are valid

	// TODO: Convert the amount to the target currency & print the result
}
